<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="refresh">@drawable/ic_refresh_24dp</item>
        <item name="settings">@drawable/ic_settings_24dp</item>
        <item name="leftArrow">@drawable/ic_keyboard_arrow_left_24dp</item>
        <item name="rightArrow">@drawable/ic_keyboard_arrow_right_24dp</item>
        <item name="share">@drawable/ic_share_24dp</item>
        <item name="bookmarks">@drawable/ic_bookmark_border_24dp</item>

    </style>

    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
    </style>

    <style name="AppTheme.Settings" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="actionBarSize">?attr/actionBarSize</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
